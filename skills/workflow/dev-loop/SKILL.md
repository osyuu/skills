---
name: dev-loop
description: >-
  一個 feature 從需求走到交付的接力:誰在哪一棒、每一棒什麼條件才算完成、實作用什麼執行器。
  當你拿到一張票或一段需求要開發、設計書寫完要開始寫 code、或**改動寫完了要交出去**時,
  主動使用——最後那個時機最容易漏,而它是回環驗收與 spec 回寫唯一的入口。
  也認 done implementing / ready for review / wrap up this change、実装できた / レビューに出したい。
  0→1 開新專案的裝配順序見 `greenfield-flow`(**只有使用者叫得到**,請他跑 `/greenfield-flow`);
  純粹「需求→設計書」用 `design-doc`;
  這支管的是**中間到交付**那段。
---

# dev-loop — 從需求到交付的接力

每一棒的做法在各自的 skill 裡,那裡才是單一真相;這支只管**交棒**與**每一棒的完成條件**。

## 第 0 棒:seam 與代價

**動手之前寫下兩件事,兩行就夠**,而且要讓使用者看得到:

- **seam**——你要在哪個公開邊界上觀察行為。`mattpocock-skills:tdd` 的說法值得照抄:
  「**Test only at pre-agreed seams.** No test is written at an unconfirmed seam.」測不完所有東西,
  先講好 seam 才能讓測試的力氣落在關鍵路徑上,而不是每一個邊角。
- **這東西壞掉的代價**——資料遺失?使用者看不到隊友?還是「一次編輯沒被複查」?
  問的是**你要寫的那個東西**壞掉會怎樣,不是它守的東西壞掉會怎樣。
  這個答案決定第 4 棒要跑到什麼程度(見〈完成的定義〉的分級)。

**沒寫下來的後果是具體的,不是形式主義。** 踩過:一次「補個閉環測試」長成 426 行的記憶體內
fabric,四輪外審、8 條發現,其中 7 條是**那個假件自己**的問題(無界等待、死欄位、註解與規格
不符)。假件壞掉不會出現在 app 裡,而它吃掉的力氣是產品程式碼的十二倍(+601 測試 / +49 產品)。
先講好 seam 的話,「要不要造這個 fabric」會是一次對話,而不是既成事實。

## 四棒

| 棒次 | 交給誰 | 這一棒完成的條件 |
|---|---|---|
| 0 定 seam 與代價 | 見上方〈第 0 棒〉 | 兩行都寫下來了,而且使用者看得到 |
| 1 收斂需求 | `design-doc` | 設計書有驗收標準,且每條**可判定**(能回答「失敗長什麼樣」) |
| 2 定順序 | 見下方〈第 2 棒〉 | 動哪些檔、什麼順序、怎麼驗,**三件都寫下來了**(回覆或 plan 檔裡看得到那三行) |
| 3 實作 | 見下方〈執行器降檔〉 | 該專案自己那套檢查全綠,**且最後一次改完之後又跑過一次** |
| 4 回環驗收 | `teammate`〈寫成「證明我錯」〉+ `design-doc` Step 5 | 見下方〈完成的定義〉 |

**第 4 棒是這支 skill 存在的主要理由。** `design-doc` 的 Step 5(回環驗收 + 回寫)寫了整整一節
論證「過期的 spec 比沒有 spec 更危險」,但它的 description 觸發詞全在**開發前**——實作完成後
沒有任何句子把它拉回來。這裡就是那句話。

## 第 2 棒:定順序

plan 不需要另一支 skill,它要的是三個答案寫下來:

- **動哪些檔**、什麼順序(有依賴的串行,無依賴的才並行)
- **怎麼驗**——每一步的驗證方式,不是「寫完再說」
- **夾不夾 codegen / migration** 這類 barrier(它決定能不能並行)

寫得出來就往下走;寫不出來表示第 1 棒還沒收斂,回去補設計書。

## 執行器降檔

**預設往低檔走**,plan 的形狀決定執行器,不是反過來:

- plan 是**一條垂直切片**(同一條功能貫穿它會經過的每一層,共享型別與狀態——可能是資料層
  到畫面,也可能是 parser 到輸出、或一支 CLI 從參數解析到副作用)→ **主線直改**,
  或想保持主線乾淨就開**單一 sub-agent**。多數 feature 屬此。
- plan 攤開後裂得成 **2 條以上互不依賴、各動各的檔**的並行流,且每塊夠大攤平 worktree 開銷
  → 才升 **agent team + worktree**(房規見 `teammate`)。開一個 worktree 要跑哪幾步是
  **per-repo 的**(有的近乎零成本,有的要重跑依賴、codegen、憑證拷貝才跑得起來),那筆開銷
  記在 `repo-onboard` 寫的 profile 裡,別用一個預設數字代替它。

「寫了設計書」不構成升 team 的理由——清楚的 plan 通常反而印證這是一條連貫切片。
單一 feature 本質環環相扣、反並行,硬拆會換來 context 碎裂 + 整合漂移,協調開銷大於並行收益。

## 完成的定義

**三條全滿足才叫「完成」。** 只滿足第 1、3 條時仍然可以交付,但那時說的是
「**改完了,這份沒有外審**」——**「完成」這個詞留給三條都滿足的情況**。
缺哪條就明說缺哪條;空著等於替讀的人下了那個判決。

1. **驗收標準逐條有結論**——通過 / 不通過 / 跑不了＋為什麼。**空白等於沒跑**,不是預設通過。
2. **有外審回報**(審的人沒參與實作),其中的 P0/P1 已處理,或明確標為「接受不修 ＋ 理由」。
   派工令怎麼寫、為什麼要對抗式、baseline 怎麼凍結,全在 `teammate`。

   **依代價分級——這條約束的是「審什麼」,不是「審幾輪」。**

   | 改動落在 | 第 2 條怎麼讀 |
   |---|---|
   | **產品路徑**(使用者跑得到的 code) | 照上面。輪數不設上限,`teammate` 的「硬性壓數量會壓抑覆蓋率」在這裡成立 |
   | **測試假件、harness、守門本身** | **注入驗證要,外審不要。** 注入(故意造一個該被抓到的違規,看它會不會紅)就是這一類的驗收;守門的守門要有下限,否則遞歸 |

   **代價算的是「你要寫的那個東西」壞掉的代價,不是「它守的東西」壞掉的代價。**
   假件守著核心路徑,不會讓假件變成核心路徑——假件寫錯的後果是「一個缺口沒被守到」,
   不是「使用者的位置丟了」。**這一格最容易被繞過**,而繞法很自然:指著被守的東西說
   「這很重要」,然後對守門本身跑產品級紀律。實測有人這樣讀過(對照探針裡的新版
   讀者判「代價：高(位置資料丟失)」然後對假件派了外審——那個代價是 `run()` 的,
   不是假件的)。

   **不分級的話這條會變成不會停的迴圈**,而三份文件各自都看不見:第 2 條要求 P0/P1 處理掉 →
   每個修正都是**新寫的、沒人審過的 code**(`teammate` 自己也寫了這句)→ 下一輪。踩過:
   一個測試假件跑了四輪外審,8 條發現裡 7 條是那個假件自己的問題,而它壞掉的代價是
   「一次編輯沒被複查」。**代價低的東西用產品級紀律對待,力氣會被它整包吃掉。**

   分級的判準就是第 0 棒寫下的那行代價。答不出來的話,回第 0 棒。
3. **翻案與新增決策已回寫設計書**(版本 +1)。翻案的當下就記,不要留到交付前憑記憶重建——
   那一刻 context 最稀,也最不可靠。

**沒有設計書時**(多數 bug 票與小改動都是,`design-doc` 自己也寫「解法顯而易見就別寫」):
第 1 條讀作「**票/需求裡的驗收條件逐條有結論**」,一條都找不到就明說「無可驗收條目」
——那本身是個發現;第 3 條讀作「翻案寫進 commit message」。**明說走的是哪一種**。
第 2 條不因此鬆動。

第 2 條的理由只有一句:**作者驗證不了自己沒想到的東西**。改動大小不影響這個前提。

## 交付時要說的

交付的第一句話要包含「這份**有/沒有**外審」。完整規則在 `teammate`〈二〉第 7 條的〈何時適用〉。

機械那一側是 `claim-check` 的〈正確性宣稱〉規則:下了正確性結論、而最後一次改 code 之後
一個 agent 都沒派過,就開火。**它認的是結論詞**(十來個固定字串),所以覆蓋邊界是:

- **不下結論的交付它一個字都看不到**——包含依上面那條寫成的第一句話本身。
- 詞表中英各一半,**其他語言要自己補進 `claim-check.conf`**(`CLAIM_*_CLAIM_RE`)。
  沒補的語言不是誤報,是**一次都不開火**——跟很誠實長得一樣。
- `SendMessage` 也算「派過人」,跟隊友講一句話就能讓它閉嘴。

也就是說第 2 條的執行力主要仍在紀律那一側;機械那側接得住的是最裸的一種。

## 交付前的整理

**loop 中的 commit 是 checkpoint,不是交付單位。** 一輪下來多半有好幾顆是「修上一顆的問題」,
那個歷史記錄的是迭代過程,不是變更的邏輯結構——跟 commit message 不寫會話過程是同一條。

所以在 push 之前整理一次。**整理只准做不改 tree 的事**:squash 成邏輯單元、重寫
commit message。判準是機械的——整理完 `git diff <整理前的 hash> HEAD` **必須是空的**。

**時機只有一個:所有 review 收完之後。** rebase 會改寫 hash,而 review 的範圍就釘在 hash 上——
先整理會讓還沒交付的報告連行號一起作廢。這也是為什麼 review 期間要疊新 commit 而不是 amend。

**清 pre-commit 的累積 warn 排在每一輪 review 派出去之前**(warn 會被滾過去,今天不清就永遠不清)。
它改的是 code,而 review 之後改 code 會把〈完成的定義〉第 2 條打掉:交付的東西沒人審過。
放在整理階段做就成了死結——要補審就得再派一輪,再派一輪 review 就又還沒收完、不能整理。

**寫「每一輪之前」而不是「最後一次 review 之前」,因為哪一輪是最後一輪只有事後才知道。**
review 提的修正本身也會生新的 warn,而那些修正是 review 之後動的 code,照第 2 條本來就要再
派一輪——那一輪的「派出去之前」就是清它的位置。照這個寫法「最後一次」是結果不是前提。

`claim-check` 的〈正確性宣稱〉規則認的正是「最後一次改 code 之後有沒有派過人」,
所以這不只是紀律,機械那側也會開火。

## 這個 repo 還沒配置過守門的話

先跑 `repo-onboard`(探測技術棧、配 harness、寫 per-repo 配置、注入故障驗證每一道會開火)。
沒有守門的 repo,上面第 3 棒的「全綠」只證明得了你記得跑的那幾樣。
