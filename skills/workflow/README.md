# workflow

**做事順序的 skill**：什麼時候該做什麼、以什麼形狀交出來。叫用當下做完就結束，
不在 repo 裡留下持續運作的機制（那是 `harness/`）。

涵蓋從需求收斂（`design-doc`）、專案開場順序（`greenfield-flow`）、多 agent 派工
（`teammate`）、跨 session 交接（`handoff`），到對外交付（`release-assets`）
與這個 marketplace 自身的房規（`skill-authoring`）。
