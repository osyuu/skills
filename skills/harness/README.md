# harness

**裝守門的 skill**：把一條規則變成 repo 裡會自己開火的機械檢查（pre-commit、
PostToolUse hook、Stop hook），或盤點這些檢查裝得對不對。

判準是**留下持續運作的東西**：叫用一次，之後每次 commit / 每次編輯都有人看著。
相對地，`workflow/` 的 skill 是叫用當下做完就結束。

裝一道不會開火的守門比不裝更糟——它讓人以為那條規則有人在看。所以這個桶裡的
每一支都必須能被注入故障驗證，而且 skill 本身要寫出「攔得住什麼／攔不住什麼」。
